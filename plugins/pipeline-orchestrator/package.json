{
  "name": "pipeline-orchestrator",
  "version": "2.0.0",
  "description": "Pipeline orchestration extension for omp — declarative multi-phase agent pipelines with state machines, retry, resume, and conditional routing",
  "omp": {
    "extensions": ["./index.ts"]
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
